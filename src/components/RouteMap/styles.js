import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  map: {
    height: '100%',
    width: '100%',
  },
  marker: {
    width: 50,
    height: 50,
    resizeMode: 'contain',
  },
});

export default styles;
